[plugin]

name="LiveKit WebView"
description="LiveKit voice chat via WebView2 (Windows) and WebView (Android). Wraps your Supabase edge function for room management."
author="CGLive"
version="1.0.0"
script="plugin.gd"
